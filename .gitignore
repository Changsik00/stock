.env
.env.*
!.env.example

# 키움 REST API 접근토큰 캐시 (app/clients/kiwoom.py) — 24h 유효 토큰이 평문으로 들어있음
backend/.kiwoom_token.json

__pycache__/
*.pyc
.venv/
venv/

node_modules/
dist/
build/

# GitHub Pages 정적 배포용 스냅샷 (CI가 매 배포마다 backend/scripts/export_static.py로
# 새로 생성하는 산출물 — 리포에는 커밋하지 않는다)
frontend/public/data/

.DS_Store
.serena/
